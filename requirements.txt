# Qt6 UI framework
PyQt6==6.7.0
PyQt6-Qt6==6.7.0
PyQt6-sip>=13.6.0

# Gemini AI
google-genai==2.14.0

# Windows accessibility API (Windows only)
pywinauto==0.6.8

# Environment variable loading
python-dotenv==1.0.1

# Hotkey support
keyboard==0.13.5

# Screen info utilities
screeninfo==0.8.1

# Screenshot capture (HybridResolver)
mss==9.0.2

# Image processing (HybridResolver — JPEG encoding for Gemini Vision)
Pillow>=11.0.0

# Browser Extension WebSocket Bridge
websockets>=12.0

# NOTE: pyatspi is NOT on PyPI — install via system package manager (Linux only)
# Do NOT add pyatspi here

# ── Optional features ────────────────────────────────────────────────────────
# Feature 1.5 — TTS via Windows SAPI (install if you want spoken instructions)
# pywin32

# Feature 1.6 — Voice input (install both to enable the microphone button)
# speechrecognition
# pyaudio